export enum CAR_LICENCE_TYPE {
    PRIVATE = 1,
    PUBLIC_TRANSPORT = 2,
    TAXI = 3
};

export enum DRIVING_MODE {
    PASSENGER_RIDE = 1,
    DELIVERY = 2,
    BOTH = 3,
};

export enum ReviewExternalType {
    Rider = 1,
    Captain = 2
}

export enum UserExternalType { // need to remove above will do later 
    Rider = 1,
    Captain = 2
}