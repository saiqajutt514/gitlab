export enum CAR_LICENCE_TYPE {
    PRIVATE = 1,
    PUBLIC_TRANSPORT = 2,
    TAXI = 3
};

export enum DRIVING_MODE {
    PASSENGER_RIDE = 1,
    DELIVERY = 2,
};

export enum EARNING_DURATION {
    TODAY = 1,
    WEEKLY = 2,
    MONTHLY = 3,
    LIFETIME = 4,
};