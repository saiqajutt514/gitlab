export enum USER_TYPE {
    CUSTOMER = 1,
    CAPTAIN = 2,
}

export enum SUBSCRIPTION_TYPE {
    MONTHLY = 1,
    YEARLY = 2,
}

export enum SUBSCRIPTION_STATUS {
    ACTIVE = 1,
    OVERDUE = 2,
    EXPIRED = 3,
    CANCEL = 4
}

